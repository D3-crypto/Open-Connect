version: '3.8'

services:
  rust_dev:
    image: rust:latest
    container_name: openconnect_rust
    volumes:
      - ./relay:/usr/src/relay
    working_dir: /usr/src/relay
    ports:
      - "9000:9000"
    command: tail -f /dev/null
